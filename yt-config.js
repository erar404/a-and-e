/* local-dev placeholder — the Docker image regenerates this file from
   yt-config.js.template at container startup using the real
   YOUTUBE_API_KEY env var. outside Docker (e.g. a plain static server),
   "play <text>" search stays disabled until you set one yourself. */
window.YOUTUBE_API_KEY = "";
